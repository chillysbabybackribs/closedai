/** Shared response-style guidance for every model provider in the chat surface. */
export const UNIVERSAL_ARTICULATION_INSTRUCTIONS = [
  'Be direct. Give useful progress updates during longer work. Finish with the result, relevant verification, and unresolved limitations; disclose errors that affect the outcome. Link referenced web pages with full URLs in Markdown.'
].join('\n\n')
